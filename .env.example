# BionluKontor Projesi - Production Environment Variables Şablonu
# DİKKAT: Bu dosyayı kendi sunucunuza kopyaladıktan sonra adını `.env` olarak değiştirin ve boşlukları kendi bilgilerinizle doldurun!

# 1. Django Güvenlik Ayarları
# DEBUG modu canlı ortamda (production) KESİNLİKLE False olmalıdır!
DEBUG=False
# Rastgele, uzun ve tahmin edilemez bir anahtar girin (Örn: terminalde `openssl rand -base64 32` çalıştırıp buraya yapıştırın)
SECRET_KEY=buraya_guclu_bir_django_secret_key_girin
# Sunucu IP adresiniz veya domain adınız (Örn: 192.168.1.5, example.com, allow all için * yazılabilir)
ALLOWED_HOSTS=*

# 2. API Anahtarları (3. Parti Servisler)
CAPTCH_API_KEY=buraya_2captcha_api_anahtarinizi_girin

# 3. Veritabanı Bilgileri (PostgreSQL)
# Bu bilgiler docker-compose.yml içerisindeki db servisi ile eşleşmelidir.
POSTGRES_DB=kontor_db
POSTGRES_USER=postgres_kullanici_adiniz
POSTGRES_PASSWORD=postgres_cok_guclu_sifreniz

# Not: Uygulama ayağa kalktığında DATABASE_URL otomatik olarak yukarıdaki bilgilerle oluşturulur.
